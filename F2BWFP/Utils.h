#pragma once

//#include <stdio.h>
//#include <fwpmu.h>

using namespace System;

namespace F2B {
	//String^ GetErrorText(DWORD rc);
	////WCHAR * GetErrorText(DWORD rc);
	//WCHAR * FormatErrorText(WCHAR * msg, DWORD rc);
}
